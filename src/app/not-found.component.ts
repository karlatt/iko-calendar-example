import { Component } from '@angular/core';

@Component({
  template: `<h2>Bon , là on a juste un texte tout pourri , mais en fait c'est du html donc
  On peut mettre le template qu'on veut ou quoi que ce soit
    parce que ici c'est le message qui dit PAGE NOT FOUNT !!!</h2>`
})
export class PageNotFoundComponent {}
